CREATE TABLE STUDENT (
    STUDENT_ID INT PRIMARY KEY,
    STUDENT_NAME VARCHAR(35) NOT NULL,
    COURSE VARCHAR(30),
    YEAR_LEVEL INT,
    hno INT REFERENCES HOSTEL(hno),
    rno INT REFERENCES ROOM(rno),
    ADMISSION_DATE DATE,
    TOTAL_ANNUAL_FEE DECIMAL(10,2)
);